<!-- The two style names below come from Pandoc's default reference document, which is
     what Paperflow uses until word.reference_docx is configured. A publisher template
     usually names its styles differently -- Frontiers, for example, has "Author List"
     rather than "Author" -- and Doctor will report the mismatch as soon as you
     configure one.

     That report is expected while this neutral example is still in place. Replace this
     file with your own front matter, or generate it from the template with
     'paperflow template-sections --split', and the mismatch disappears.
     'paperflow template-styles' lists the names a template actually offers. -->

::: {custom-style="Title"}
A neutral Paperflow demonstration document
:::

::: {custom-style="Author"}
Example Author
:::
